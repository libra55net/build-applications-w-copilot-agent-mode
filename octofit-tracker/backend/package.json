{
  "name": "octofit-tracker-backend",
  "version": "1.0.0",
  "private": true,
  "main": "dist/server.js",
  "scripts": {
    "dev": "PORT=8000 ts-node-dev --respawn --transpile-only src/server.ts",
    "build": "tsc",
    "test": "npm run build",
    "start": "PORT=8000 node dist/server.js",
    "seed": "npm run build && node dist/scripts/seed.js"
  },
  "dependencies": {
    "express": "^5.2.1",
    "mongoose": "^9.8.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^26.1.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^7.0.2"
  }
}
